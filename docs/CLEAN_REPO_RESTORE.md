# 🧹 Восстановление проекта с чистой историей

## План действий

1. ✅ Удалить текущий репозиторий на GitHub
2. ✅ Создать новый репозиторий с тем же названием
3. ✅ Удалить `.git` из локального проекта
4. ✅ Инициализировать новый git репозиторий
5. ✅ Сделать один красивый initial commit
6. ✅ Push в новый репозиторий

## ⚠️ Важно про файлы

**Все ваши локальные файлы останутся:**
- ✅ Untracked файлы (с отметкой `??`) - останутся локально, **НЕ попадут в репозиторий**
- ✅ Измененные файлы (с отметкой `M`, например `main.tf`) - будут добавлены в коммит
- ✅ Отслеживаемые файлы проекта - будут добавлены в коммит

**Что удалится:**
- ❌ Только `.git` директория (история коммитов)
- ❌ Репозиторий на GitHub (но создадим новый с тем же названием)

## Пошаговая инструкция

### Шаг 1: Удалить репозиторий на GitHub

1. Перейдите в репозиторий на GitHub
2. **Settings** → прокрутите вниз до **Danger Zone**
3. Нажмите **Delete this repository**
4. Введите название репозитория для подтверждения
5. Нажмите **I understand the consequences, delete this repository**

### Шаг 2: Создать новый репозиторий с тем же названием

1. На GitHub нажмите **New repository**
2. Название: `flask-app-devops` (то же самое)
3. **НЕ** добавляйте README, .gitignore, license
4. Нажмите **Create repository**

### Шаг 3: Очистить локальный проект от git истории

```bash
cd /home/barem/flask-app-devops

# Удалить .git директорию (вся история коммитов)
rm -rf .git

# Проверить, что .git удалена
ls -la | grep .git  # Не должно ничего показать
```

**Важно:** Все ваши локальные файлы останутся на месте. После `git add .` проверьте `git status` и если какие-то untracked файлы попали, которые вы не хотите в репозитории, уберете их командой `git reset HEAD <файл>`. Эти файлы останутся локально, но не попадут в коммит.

### Шаг 4: Инициализировать новый git репозиторий

```bash
# Инициализировать новый репозиторий
git init

# Добавить все файлы проекта (кроме тех, что в .gitignore)
git add .

# Проверить, что добавлено
git status

# Если какие-то untracked файлы попали, которые вы НЕ хотите в репозитории, убрать их:
# git reset HEAD <файл>  # для конкретного файла
# Например:
# git reset HEAD DIAGNOSE_504.md
# git reset HEAD ansible/inventory.ini.10784.2025-11-14@11:26:33~

# После этого эти файлы останутся локально, но не попадут в коммит

# Сделать один красивый initial commit
git commit -m "Initial commit: Flask DevOps project with full CI/CD pipeline

- Infrastructure as Code with Terraform
- Configuration Management with Ansible
- CI/CD pipeline with GitHub Actions
- Docker containerization
- Monitoring stack (Prometheus, Grafana, Loki)
- Automated backup system
- Nginx reverse proxy with SSL"

# Добавить remote
git remote add origin https://github.com/Kartvel97/flask-app-devops.git

# Push в новый репозиторий
git branch -M main
git push -u origin main
```

### Шаг 5: Проверить, что всё работает

```bash
# Проверить статус
git status

# Проверить историю (должен быть только один коммит)
git log --oneline

# Проверить remote
git remote -v
```

## Что НЕ нужно делать

❌ **Не нужно** восстанавливать из бэкапа - у вас уже есть все файлы локально  
❌ **Не нужно** клонировать что-то - просто удалите `.git` и создайте новый  
❌ **Не нужно** менять названия файлов или структуру проекта  

## Что останется без изменений

✅ Все файлы проекта  
✅ AWS инстанс и конфигурации  
✅ Elastic IP  
✅ Ansible inventory  
✅ Все настройки на сервере  

## Обновление на сервере (если нужно)

После создания нового репозитория, на сервере нужно обновить remote URL:

```bash
# Подключиться к серверу
ssh -i ~/.ssh/flask_app_key ubuntu@52.29.151.247

# Перейти в директорию проекта
cd /opt/flask-app

# Обновить remote URL (если изменился)
sudo git remote set-url origin https://github.com/Kartvel97/flask-app-devops.git

# Или просто пересоздать (если хотите чисто)
sudo rm -rf /opt/flask-app
# Затем запустить Ansible playbook - он склонирует новый репозиторий
```

Или просто запустить Ansible playbook заново:

```bash
cd /home/barem/flask-app-devops/ansible
ansible-playbook -i inventory.ini deploy-flask-app.yml
```

## Альтернатива: Без удаления репозитория

Если не хотите удалять репозиторий, можно просто перезаписать историю:

```bash
cd /home/barem/flask-app-devops

# Удалить .git
rm -rf .git

# Инициализировать новый
git init
git add .
git commit -m "Initial commit: Flask DevOps project with full CI/CD pipeline"

# Force push (перезапишет историю)
git remote add origin https://github.com/Kartvel97/flask-app-devops.git
git branch -M main
git push -f origin main
```

⚠️ **Внимание:** `git push -f` перезапишет всю историю. Если репозиторий публичный и кто-то его форкнул, лучше удалить и создать новый.

## Итог

После выполнения этих шагов у вас будет:
- ✅ Чистая история коммитов (один initial commit)
- ✅ Тот же репозиторий с тем же названием
- ✅ Все файлы на месте
- ✅ Никакой "каши" в истории

**Время выполнения:** ~5 минут

