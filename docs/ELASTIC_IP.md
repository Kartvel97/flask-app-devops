# 🌐 Elastic IP - Статический IP адрес в AWS

## Что такое Elastic IP?

**Elastic IP (EIP)** — это статический публичный IPv4 адрес, который AWS выделяет для вашего аккаунта. В отличие от обычного динамического IP адреса, Elastic IP **не меняется** при перезагрузке или перезапуске EC2 инстанса.

## Проблема без Elastic IP

### Динамический IP (без Elastic IP):
- ❌ IP адрес меняется при каждом `stop/start` инстанса
- ❌ Нужно обновлять DNS записи в Cloudflare после каждой перезагрузки
- ❌ Нужно обновлять конфигурации, inventory файлы, secrets
- ❌ Неудобно для production окружений

### Статический IP (с Elastic IP):
- ✅ IP адрес **никогда не меняется**
- ✅ Можно один раз настроить DNS и забыть
- ✅ Удобно для production
- ✅ Можно переносить между инстансами

## Как это работает?

### 1. Выделение Elastic IP

```bash
aws ec2 allocate-address --domain vpc --region eu-central-1
```

AWS выделяет вам IP адрес из своего пула. Этот IP адрес **принадлежит вам** до тех пор, пока вы его не освободите.

### 2. Привязка к инстансу

```bash
aws ec2 associate-address \
  --instance-id i-1234567890abcdef0 \
  --allocation-id eipalloc-1234567890abcdef0
```

Elastic IP привязывается к сетевому интерфейсу вашего EC2 инстанса. Теперь весь трафик, идущий на этот IP, направляется на ваш инстанс.

### 3. Что происходит при перезагрузке?

**С динамическим IP:**
```
Инстанс запущен → IP: 3.67.193.174
Инстанс остановлен → IP освобождается
Инстанс запущен снова → IP: 52.29.151.247 (НОВЫЙ!)
```

**С Elastic IP:**
```
Инстанс запущен → IP: 52.29.151.247
Инстанс остановлен → IP остается за вами
Инстанс запущен снова → IP: 52.29.151.247 (ТОТ ЖЕ!)
```

## Архитектура в нашем проекте

### Terraform конфигурация

```hcl
# 1. Создаем Elastic IP
resource "aws_eip" "flask_app_eip" {
  domain = "vpc"
  tags = {
    Name = "flask-app-devops-eip"
  }
}

# 2. Создаем EC2 инстанс
resource "aws_instance" "flask_server" {
  # ... конфигурация инстанса
}

# 3. Привязываем Elastic IP к инстансу
resource "aws_eip_association" "flask_app_eip_assoc" {
  instance_id   = aws_instance.flask_server.id
  allocation_id = aws_eip.flask_app_eip.id
}
```

### Что происходит при `terraform apply`:

1. Terraform создает Elastic IP → AWS выделяет IP адрес
2. Terraform создает EC2 инстанс → AWS запускает виртуальную машину
3. Terraform привязывает EIP к инстансу → IP адрес становится доступным

### Что происходит при `terraform destroy`:

1. Terraform отвязывает EIP от инстанса
2. Terraform удаляет инстанс
3. **Важно:** Elastic IP остается в вашем аккаунте (чтобы не потерять его, нужно явно освободить)

## Важные моменты

### 💰 Стоимость

- **Бесплатно**, если Elastic IP привязан к работающему инстансу
- **$0.005/час** (~$3.6/месяц), если Elastic IP не привязан к инстансу
- **Бесплатно**, если у вас есть несколько Elastic IP на один инстанс (но обычно это не нужно)

### 🔄 Перенос между инстансами

Elastic IP можно легко переносить между инстансами:

```bash
# Отвязать от старого инстанса
aws ec2 disassociate-address --association-id eipassoc-xxx

# Привязать к новому инстансу
aws ec2 associate-address \
  --instance-id i-new-instance \
  --allocation-id eipalloc-xxx
```

### 🚨 Ограничения

- Максимум 5 Elastic IP на аккаунт по умолчанию (можно увеличить через support)
- Elastic IP работает только в рамках одного региона
- При удалении инстанса EIP автоматически не освобождается

## В нашем проекте

### Текущая конфигурация:

- **Elastic IP:** `52.29.151.247`
- **Allocation ID:** `eipalloc-0cfad3da97b0b9933`
- **Инстанс:** `i-045f5c0961880b55c`

### Где используется:

1. **Cloudflare DNS** — A запись указывает на этот IP
2. **Ansible inventory** — `ansible/inventory.ini`
3. **Terraform output** — `terraform output public_ip`
4. **GitHub Actions secrets** — `AWS_HOST` (нужно обновить вручную)

### Обновление GitHub Secrets:

Если вы используете GitHub Actions для CI/CD, обновите secret `AWS_HOST`:

1. Перейдите в GitHub → Settings → Secrets and variables → Actions
2. Найдите `AWS_HOST`
3. Обновите значение на `52.29.151.247`

## Команды для работы с Elastic IP

### Проверить текущий Elastic IP:

```bash
aws ec2 describe-addresses \
  --filters "Name=instance-id,Values=i-045f5c0961880b55c" \
  --query 'Addresses[0].PublicIp' \
  --output text
```

### Создать и привязать Elastic IP (через Ansible):

```bash
cd ansible
ansible-playbook attach-elastic-ip.yml
```

### Освободить Elastic IP (если больше не нужен):

```bash
aws ec2 release-address --allocation-id eipalloc-0cfad3da97b0b9933
```

⚠️ **Внимание:** После освобождения IP адрес будет потерян навсегда!

## Преимущества для нашего проекта

1. ✅ **Стабильность** — IP не меняется при перезагрузках
2. ✅ **Удобство** — не нужно обновлять Cloudflare после каждого рестарта
3. ✅ **Надежность** — можно быстро перенести на другой инстанс при проблемах
4. ✅ **Production-ready** — стандартная практика для production окружений

## Сравнение: до и после

### До (динамический IP):
```
Перезагрузка → IP меняется → Обновить Cloudflare → Обновить inventory → Обновить secrets
```

### После (Elastic IP):
```
Перезагрузка → IP остается тем же → Ничего делать не нужно! ✅
```

